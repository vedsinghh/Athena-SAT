# Landing page assets

Drop files here with these exact names and the placeholders on the landing page
disappear automatically (each slot falls back to a dashed box until the file exists).

| File | Where it appears | Suggested size | Notes |
|------|------------------|----------------|-------|
| `hero-dashboard.png` | Hero browser frame | 1440 × 900 | Screenshot of the signed-in dashboard. Take it at a wide window, light mode, with a profile that has real streak/accuracy numbers. |
| `shot-practice.png` | "Timed practice" section | 1400 × 900 | A Math question mid-session with the Desmos calculator open and the timer visible. |
| `shot-qbank.png` | "Question bank" section | 1400 × 900 | Question Bank page showing domains/skills with filters applied. |
| `shot-analytics.png` | "Analytics" section | 1400 × 900 | Analytics page with the accuracy breakdown and streak card. |
| `avatar-1.jpg` | Testimonial 1 | 240 × 240 | Square, will be cropped to a circle. |
| `avatar-2.jpg` | Testimonial 2 | 240 × 240 | Square. |
| `avatar-3.jpg` | Testimonial 3 | 240 × 240 | Square. |

## Screenshot tips

- Use a 1440px-wide browser window and a 2× display so the PNGs stay crisp.
- Hide any personal email or real name before capturing.
- Keep the same profile across all four screenshots so the numbers tell one story.

## Already using real art

These come from `/public` and need nothing from you:

- `/athena.png` — hero mascot
- `/athena-coach.png` — "How it works" section
- `/athena-math.png`, `/athena-rw-tab.png`, `/athena-progress.png` — showcase rows
- `/athena-throwing.png` — final call-to-action band

## Still to decide

- Real testimonial quotes (the three on the page are placeholders and are labeled as such).
- Optional: a school/trust logo strip if you get partner schools.
